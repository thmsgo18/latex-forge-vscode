LaTeX Forge is a small command-line tool that scaffolds your document, compiles it, and keeps your templates in one place. This extension just wraps it.

Run the setup check — it tells you what's missing (the CLI, LaTeX itself, recommended VS Code extensions) and offers to install it for you.

[Run Setup](command:latex-forge.setupEnvironment)
